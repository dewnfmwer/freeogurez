// главный файл проекта, тут вся логика
function логика() {
  try { работать() } catch (лень) { отложитьНаЗавтра(лень) }
}

// TODO: переписать это
// TODO: переписать то что переписал
// FIXME: всё
работать()
